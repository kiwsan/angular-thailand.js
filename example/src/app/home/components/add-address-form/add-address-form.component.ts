class AddAddressFormController {
}

export class AddressForm implements angular.IComponentOptions {
    static selector = 'addressForm';
    static controller = AddAddressFormController;
    static template = require('./add-address-form.component.html');
}
